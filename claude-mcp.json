{
  "mcpServers": {
    "agentic-minions": {
      "command": "go",
      "args": ["run", "."],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "GATEWAY_URL": "${user_config.gateway_url}"
      }
    }
  }
}
