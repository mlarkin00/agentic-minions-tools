{
  "mcpServers": {
    "agentic-minions": {
      "command": "sh",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/launch.sh"],
      "env": {
        "GATEWAY_URL": "${user_config.gateway_url}"
      }
    }
  }
}
